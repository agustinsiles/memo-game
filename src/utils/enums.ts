export enum GameStatus {
  IN_PROGRESS,
  OVER,
  NOT_STARTED,
}

export enum GameCategories {
  SPORTS = "SPORTS",
  MUSIC = "MUSIC",
}

export enum ColorVariants {
  DEFAULT = "default",
  PRIMARY = "primary",
  SUCCESS = "success",
  DANGER = "danger",
}
